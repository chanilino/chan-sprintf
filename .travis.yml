language: c
compiler:
        - gcc
before_install:
  - sudo add-apt-repository 'ppa:george-edison55/precise-backports' -y
  - sudo apt-get update -q
  - sudo apt-get install cmake check -y


script: mkdir -p tests/build && cd tests/build && cmake .. && make && ./tests-printf 

